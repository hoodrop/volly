version: "2"

run:
  timeout: 5m

linters:
  # The "standard" default set (errcheck, govet, ineffassign, staticcheck,
  # unused) stays enabled; the ones below are added on top.
  enable:
    - misspell

formatters:
  enable:
    - gofmt
